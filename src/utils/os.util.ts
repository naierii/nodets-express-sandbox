/**
 * Information about or interact with my system
 */

import os from 'os';

// userInfo()
console.log(os.userInfo());

// totalMem()
console.log(os.totalmem());

// freemem()
console.log(os.freemem());

// cpus()
console.log(os.cpus());